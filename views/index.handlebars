<h1>Stop, burgertime</h1>

<h2>Undevoured burgers</h2>

<ul>
    {{#each burgers}}
        {{#unless devoured}}
            {{>burger_name/burger-block devoured=true}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Devoured burgers</h2>

<ul>
{{#each burgers}}
{{#if devoured}}
   {{>burger_name/burger-block devoured=false}}
{{/if}}
{{/each}}
</ul>

<div class="finish">
  <button type="submit">Devour it</button>
</div>